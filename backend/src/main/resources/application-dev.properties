# Development Database Configuration (H2 for local development)
quarkus.datasource.db-kind=h2
quarkus.datasource.username=sa
quarkus.datasource.password=
quarkus.datasource.jdbc.url=jdbc:h2:mem:inventorydb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE

# Hibernate Configuration
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.log.sql=true

# CORS Configuration
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:3000
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with

# Logging
quarkus.log.level=INFO

# Uncomment below to use Oracle in development (requires Oracle running)
# quarkus.datasource.db-kind=oracle
# quarkus.datasource.username=inventory_user
# quarkus.datasource.password=inventory_pass
# quarkus.datasource.jdbc.url=jdbc:oracle:thin:@localhost:1521:XE

